let valores = [1,3,7,4,9,6]

/*
for(
	let pos=0;
	pos < valores.length;
	pos++
	){
	console.log(
		`Posição ${pos} com o Valor ${valores[pos]}`
		 )
		 
	//console.log(valores[pos])
}*/

for(pos in valores){
	console.log(valores[pos])
}
